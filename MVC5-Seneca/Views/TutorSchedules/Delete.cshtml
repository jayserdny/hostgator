@model MVC5_Seneca.EntityModels.TutorSchedule
@{
    Layout = null;
}  
<!DOCTYPE html>
<html>
    <head>
        <meta name="viewport" content="width=device-width" />
        <title>Delete</title>
    </head>
    <body>
        <h3>Are you sure you want to delete this?</h3>
        <div>
            @using (Html.BeginForm())
            {
                <h4>Tutor Schedule</h4>
                <hr />
                <table>
                    <tr>
                        <td><b>@Html.DisplayNameFor(model => model.Tutor)</b></td>
                        <td>@Html.DisplayFor(model => model.Tutor.FullName)</td>
                    </tr>
                    <tr>
                        <td><b>@Html.DisplayNameFor(model => model.Student)</b></td>
                        <td>@Html.DisplayFor(model => model.Student.FirstName)</td>
                    </tr>
                    <tr>
                        <td><b>@Html.DisplayNameFor(model => model.DayName)</b></td>
                        <td>@Html.DisplayFor(model => model.DayName)</td>
                    </tr>
                    <tr>
                        <td><b>@Html.DisplayNameFor(model => model.TimeOfDay)</b></td>
                        <td>@Html.DisplayFor(model => model.TimeOfDay)</td>
                    </tr>
                </table> 
                <br />
                @Html.AntiForgeryToken()
                <div>
                    <table>
                        <tr><td><input type="submit" value="Delete" /></td></tr>
                        <tr></tr>
                        <tr></tr>
                        <tr><td>@Html.ActionLink("Back to List", "Index")</td></tr>
                    </table>
                </div>
            }
        </div>
    </body>
</html>
