@model MVC5_Seneca.EntityModels.User

@{
    Layout = null;
    if (Session["userId"] == null)
    {
        Response.Redirect("~/Login.Index");
    }
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Login</title>
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
    @*Put page javascript in a separate .js file where breaks can be set during debugging:*@
    @*<script src="@Url.Content("/Scripts/UserInfo.js")"></script>*@
</head>
<body>

    <div>
        <table>
            <tr>
                <td>&nbsp; User Name:&nbsp @Session["userName"].ToString() </td>
            </tr>
            <tr>
                <td style="text-decoration:underline"><strong>&nbsp;Seneca Heights Education Program Dashboard</strong></td>
            </tr>
            <tr></tr>
        </table>
    </div>

    @if ((bool)Session["Administrator"] == true)
    {
    <div class="col-md-3">
        Administator Menu<br />
        <a href="@Url.Action("MaintainUsers", "Home")">Maintain Users</a><br />
        <a href="@Url.Action("MaintainStudents", "Home")">Maintain Students</a><br />
        <a href="@Url.Action("MaintainParents", "Home")">Maintain Parents</a><br />
        <a href="@Url.Action("MaintainStaff", "Home")">Maintain Staff</a><br />
        <a href="@Url.Action("MaintainDocumentTypes", "Home")">Maintain Document Types</a><br />
        <a href="@Url.Action("MaintainStudentReports", "Home")">Maintain Student Reports</a><br />
        <a href="@Url.Action("UploadStudentReports", "Home")">Upload Student Reports</a><br />
        <a href="@Url.Action("MaintainSchools", "Home")">Maintain Schools</a><br />
        <a href="@Url.Action("MaintainUserRoles", "Home")">Maintain UserRoles</a><br />
        <a href="@Url.Action("MaintainTutorNotes", "Home")">Maintain TutorNotes</a><br />
        <a href="@Url.Action("ResetAnyPassword", "Home")">Reset Any Password</a><br />
        <br />
    </div>
    }

        <div class="col-md-3">
            Tutor Menu<br />
            <a href="@Url.Action("DisplayStudentInfo", "Home")">Display Student Info</a><br />
            <a href="@Url.Action("ChangeMyPassword", "Home")">Change My Password</a><br />
            <a href="@Url.Action("Logout", "Home")">LogOut</a>
        </div>

</body>
</html>
